<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN " "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.boardserver.mapper.PostSearchMapper">
    <select id="selectPosts" resultType="com.example.boardserver.dto.request.PostSearchRequest">
        SELECT
        FROM "post"
        WHERE 1=1
        <if test="name != null and name != ''" >
            AND name LIKE CONCAT(#{name}, '%')
        </if>
        <if test="contents != null and name != ''" >
            AND name LIKE CONCAT(#{contents}, '%')
        </if>
        <if test="categoryId != null and name != ''" >
            AND categoryId = #{categoryId}
        </if>
        <if test="sortStatus.toString() == 'NEWEST'" >
            ORDER BY createTime DESC
        </if>

        <if test="sortStatus.toString() == 'OLDEST'" >
            ORDER BY createTime ASC
        </if>

        <if test="sortStatus.toString() == 'CATEGORIES'" >
            ORDER BY categoryId
        </if>



    </select>


    <insert id="register" parameterType="com.example.boardserver.dto.PostDTO">
        INSERT INTO category(id,name)
        VALUES(#{id},#{name})
    </insert>

    <update id="updateCategory" parameterType="com.example.boardserver.dto.PostDTO">
        UPDATE category
        SET name = #{name}
        WHERE id = #{id}
    </update>




</mapper>